import pixel

@Namespace .

function clear(); => {
    clear
};


function draw_pixel(num x,y); => {
    pixel.drawchar x:x; y:y; color:white; char:#;
    suppress_errors()
};

function random(num min, num max);{
    num randomized >>rand:$min$,$max$
    return randomized;
};
